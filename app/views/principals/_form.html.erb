<%= form_for @principal, :html => { :class => "form-horizontal principal" } do |f| %>

  <% if @principal.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@principal.errors.count, "error") %> prohibited this principal from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @principal.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :academico, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :academico, :class => 'form-control' %>
    </div>
    <%=f.error_span(:academico) %>
  </div>
  <div class="form-group">
    <%= f.label :nombre, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :nombre, :class => 'form-control' %>
    </div>
    <%=f.error_span(:nombre) %>
  </div>
  <div class="form-group">
    <%= f.label :apellido, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :apellido, :class => 'form-control' %>
    </div>
    <%=f.error_span(:apellido) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                principals_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>

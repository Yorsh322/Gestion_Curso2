<%= form_for @boss, :html => { :class => "form-horizontal boss" } do |f| %>

  <% if @boss.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@boss.errors.count, "error") %> prohibited this boss from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @boss.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <label class="control-label col-lg-2">Nivel acad&eacute;mico</label>
    <div class="col-lg-10">
      <%= f.text_field :academico, :class => 'form-control' %>
    </div>
    <%=f.error_span(:academico) %>
  </div>
  <div class="form-group">
    <%= f.label :nombre, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :nombre, :class => 'form-control' %>
    </div>
    <%=f.error_span(:nombre) %>
  </div>
  <div class="form-group">
    <%= f.label :apellido, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :apellido, :class => 'form-control' %>
    </div>
    <%=f.error_span(:apellido) %>
  </div>

    <div class="form-group">
      <%= f.label 'Departamento', :class => 'control-label col-lg-2' %>
      <div class="col-lg-10">
        <%= f.text_field :department, :class => 'form-control' %>
      </div>
      <%=f.error_span(:department) %>
    </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit 'Guardar', :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                bosses_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
